org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G
android.useAndroidX=true
android.newDsl=false
# Flutter 3.47.3 currently rejects AGP's built-in Kotlin 2.2.10 as below its
# own 2.2.20 minimum (flutter/flutter#192167). Remove this opt-out after that
# upstream issue is fixed and all Android plugins have migrated.
android.builtInKotlin=false
