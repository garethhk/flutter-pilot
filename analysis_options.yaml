include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
linter:
  rules:
    - use_build_context_synchronously
    - cancel_subscriptions
    - close_sinks
    - discarded_futures
    - unawaited_futures
